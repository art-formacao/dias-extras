/* Configuração pública para a GitHub Contents API.
 * Estes valores não são segredos. O token é pedido pela app e fica apenas
 * no localStorage deste dispositivo; nunca o coloque neste ficheiro.
 */
window.GITHUB_CONFIG = {
  owner: 'art-formacao',
  repo: 'dias-extras',
  branch: 'main',
  path: 'Disco.xlsx'
};
